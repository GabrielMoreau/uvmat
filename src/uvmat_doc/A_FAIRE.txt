
-fix ne marche pas en batch avec mask, ni avec le test de borne sup et ref file. A verifier. 

-get_field: gérer figures avec interface specifique, reglage des parametrees, voir champs attachs a la figure

-plot_object: probleme lorsqu'on ouvre un nouveau fichier avec un patch present, blocage Matlab. (voir CData et CAlphadata)

-proj_field: 
        _revoir projection des champs maillés: interpoler lineairement
	-faire projections selon lignes en 3D
	-faire projection simultanee sur plusieurs plans (eg hori et vert)

-phys: -revoir projection des champs maillés: interpoler lineairement

-read_civxdata:  lire les patch sous forme de matrice reguliere ?

-series: revoir le cas avec projection sur patch (histograms)
   revoir projection sur ligne

-uvmat: voir mise a jour du masque lorsqu'on change de fichier
   mettre un message d'avertissement pour PLOT d'un objet qui n'est pas en mode edition
   mettre un message pour makemask en coordonnées phys
 mettre des tablaux pour geometry_calib et objjets (uitable)
  amliorer lecture fichier Imadoc (vitesse)
   

- voir ** dans uvmat_doc
          
-civ:
 	*passer civ en xml.
      message Jan bert Flor wrote:
> Des calcules repetitives en CIV1 et CIV2 diminuent la taille du champ de vitesse par en bas.
> J'ai commencer avec un champ allant de 30 jusqu'a 990 px (image 1024x1024) et est maintenant en 200-890.
> Refaire un calcule en CIV1 ne fait pas revenir en arriere. Les images faites par un seul calcule change
> peu la dimension. (le decimal shift (on ou off) ne change rien). Est cela un bug?
Je crois me rappeler que cela arrive quand tu as trop de vecteurs faux sur les bords: patch va par defaut entre le min et max des vecteurs justes de civ, et tu ne peux pas faire civ2 en dehors de ces limites.
Tu peux essayer d'imposer la grille attachee. option get_grid dans l'interface civ.
(tu peux facilement creer un autre fichier grille par le TOOL 'grid' dans uvmat.) 

-patch:
faire nouveau patch:
	-pb: quand on refait patch, rho est lu sur le fichier existant, et non modifie.
	-pb des subdomains avec mask
	-exclure vecteurs qui s'ecartent
	-imposer dx plutot que nx, grille imposee. Produire une matrice rectangulaire.
	-pb pour nx=256
Louis Gostiaux wrote:
> Salut Joel,
> Je viens de me rendre compte que pour une même manip, la grille du patch varie d'un fichier nc à l'autre ??? J'ai toujours considéré que c'était une grille régulière CONSTANTE dans le temps, apparemment ce n'est pas le cas ? Tu me confirmes bien cela ?
> Merci,
> Louis.
Oui c'est un probleme que la nouvelle version de patch doit regler, a voir avec Gauthier. 

-------------------------------------------
Changes made: uvmat2.1 -> 2.2:
- reading avi movies using mmreader (for version 2009 of matalb)
- rationalisation of netcdf reading and writing functions, using functions UVMAT/nc2struct and UVMAT/struct2nc. Use of builtin netcdf Matlab function when available (version 2009)
- replacement of all functions using image processing toolbox.
- improvement of vector color representation
- improvement of the get_field interface to scan and plot fields from general netcdf files 

